# Example Vite env variables for local development
# Copy to `.env` in project root (Vite loads .env) and adjust values

# Backend API (local backend):
VITE_BACKEND_URL=http://localhost:3000

# Mix ID (external service) settings (optional):
VITE_MIX_ID_API_BASE=https://data-center.zorin.cloud/api
VITE_MIX_ID_CLIENT_ID=
VITE_MIX_ID_CLIENT_SECRET=

# ML service (optional):
VITE_ML_API_URL=https://kimai-ml.zorin.cloud

# For dev proxy to Kimai server (to avoid CORS when using /api/proxy):
VITE_KIMAI_URL=
