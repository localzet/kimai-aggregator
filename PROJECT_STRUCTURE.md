# Kimai Aggregator - Структура проекта (FSD)

## 📁 Архитектура проекта

Проект реструктурирован согласно **Feature-Sliced Design (FSD)** паттерну для улучшения масштабируемости и поддерживаемости.

### Структура папок:

```
src/
├── app/
│   └── AppShell.module.css       # AppShell стили
│
├── shared/                        # Общие ресурсы (не зависят от фич)
│   ├── api/                       # API клиенты и DB
│   │   ├── kimaiApi.ts           # Kimai API client
│   │   ├── db.ts                 # IndexedDB кэширование
│   │   └── index.ts              # Экспорты
│   │
│   ├── hooks/                     # Переиспользуемые хуки
│   │   ├── useSettings.ts        # Настройки пользователя
│   │   ├── useDashboardData.ts   # Загрузка и кэширование данных
│   │   ├── useSyncStatus.ts      # Статус синхронизации
│   │   └── index.ts              # Экспорты
│   │
│   ├── ui/                        # Переиспользуемые UI компоненты
│   │   ├── loading-screen.tsx    # Экран загрузки
│   │   ├── page.tsx              # Page wrapper с анимацией
│   │   ├── RouterLink.tsx        # Router Link adapter с forwardRef
│   │   ├── table/                # Табличные компоненты
│   │   ├── metrics/              # Метрики карточки
│   │   └── index.ts              # Экспорты
│   │
│   ├── constants/                 # Глобальные константы
│   │   └── index.ts
│   │
│   ├── types/                     # Глобальные типы
│   │   └── index.ts
│   │
│   └── utils/                     # Утилиты (форматирование, etc)
│       └── index.ts              # formatCurrency, formatDuration
│
├── entities/                      # Доменные модели и логика
│   ├── timesheet/
│   │   └── index.ts              # Timesheet типы и экспорты
│   │
│   ├── financial/
│   │   └── index.ts              # Financial типы и агрегация
│   │
│   └── settings/
│       └── index.ts              # Settings типы
│
├── features/                      # Фичи приложения (изолированы)
│   ├── dashboard/
│   │   ├── DashboardPage.tsx     # Dashboard страница
│   │   ├── components/           # Компоненты только для этой фичи
│   │   │   ├── WeekProgress.tsx
│   │   │   └── StatusIndicator.tsx
│   │   └── index.ts
│   │
│   ├── timesheet/
│   │   ├── TimesheetPage.tsx
│   │   ├── components/
│   │   │   └── TimesheetTable.tsx
│   │   └── index.ts
│   │
│   ├── financial/
│   │   ├── FinancialPage.tsx
│   │   ├── components/
│   │   │   ├── FinancialTable.tsx
│   │   │   └── FinancialMetrics.tsx
│   │   └── index.ts
│   │
│   ├── payment-history/
│   │   ├── PaymentHistoryPage.tsx
│   │   └── index.ts
│   │
│   ├── statistics/
│   │   ├── StatisticsPage.tsx
│   │   └── index.ts
│   │
│   └── settings/
│       ├── SettingsPage.tsx
│       ├── components/
│       │   └── SettingsForm.tsx
│       └── index.ts
│
├── layout.tsx                     # MainLayout компонент
├── router.tsx                     # Маршруты и ошибки
├── app.tsx                        # App провайдеры
├── main.tsx                       # Entry point
└── global.css                     # Глобальные стили
```

## ✨ Основные улучшения

### 1. **Feature-Sliced Design (FSD)**
   - Каждая фича изолирована в отдельной папке
   - Компоненты фич находятся локально (`features/*/components/`)
   - Четкое разделение concerns

### 2. **Shared ресурсы**
   - API и DB логика в `shared/api/`
   - Переиспользуемые хуки в `shared/hooks/`
   - Глобальные UI компоненты в `shared/ui/`
   - Утилиты и типы в `shared/`

### 3. **Entities слой**
   - Доменные модели (типы) в `entities/`
   - Переэкспортирование типов для удобства
   - Подготовка для будущей доменной логики

### 4. **Лучшие практики**
   - Path alias `@/` для чистых импортов
   - RouterLink adapter с `forwardRef` для работы с Mantine
   - Index файлы для публичных экспортов
   - ESLint конфигурация для качества кода

## 🚀 Команды

```bash
# Development
npm run dev          # Запуск dev сервера (Vite, порт 5174)

# Production
npm run build        # Production build
npm run typecheck    # TypeScript проверка
npm run lint         # ESLint проверка (когда настроена)

# Electron (если нужно)
npm run electron:build
```

## 📊 Статистика

- **TypeScript**: Проверка типов ✅
- **Build**: Успешная сборка (19.34s) ✅
- **Dev Server**: Запущен на порте 5174 ✅
- **Total Modules**: 8043 трансформировано

## 📁 Структура проекта

- `src/pages/` → Страницы приложения
- `src/components/` → Компоненты страниц (будут перемещены в features)
- `src/shared/` → Общий код (API, hooks, UI, utils)
- `src/widgets/` → Композитные UI компоненты
- `src/app/` → Инициализация приложения
- `src/entities/` → Доменные сущности
- Все импорты используют абсолютные пути (`@/shared/api`, `@/shared/hooks`, etc)

## 📝 Next Steps (Опционально)

1. Скопировать дополнительные UI компоненты из `remnawave/frontend` если требуется
2. Удалить старые папки когда миграция полностью завершена
3. Расширить entities с доменной логикой
4. Добавить стандартные утилиты в `shared/utils`
5. Настроить ESLint rules по предпочтениям команды
